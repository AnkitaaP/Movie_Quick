<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    package="com.ankita.moviequick">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".activity.activity.activity.activity.SearchResultsActivity"></activity>
        <activity android:name=".activity.activity.activity.activity.FavouritesActivity" />
        <activity android:name=".activity.activity.activity.activity.MovieDetailActivity" />
        <activity android:name=".activity.activity.activity.activity.SearchActivity" />
        <activity android:name=".activity.activity.activity.activity.DisplayWallActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>